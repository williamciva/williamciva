name: Update badges

on:
  schedule:
    # Runs at 0am UTC every day
    - cron: "46 9 * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: williamciva/williamciva@main
